# Rapier Physics Migration Progress

- [x] Add `@react-three/rapier` as a project dependency without changing the live physics path yet | AC: `package.json` and `package-lock.json` include `@react-three/rapier`, current source still compiles, and `npm run typecheck && npm run lint && npm run build` pass
- [x] Extract current pool floor and wall measurements into a shared typed physics constants module | AC: one typed module defines the current floor position/size and all four wall positions/sizes, and duplicate bound numbers are removed from the physics migration path
- [x] Extract shared item physics constants for spawn height, center target, and steering strength | AC: spawn, center-target, and steering scalar values live in the shared physics constants module and `Items` migration work can import them without reintroducing magic numbers
- [x] Add a Rapier floor collider component that mirrors the current pool bottom | AC: a Rapier fixed collider exists for the pool floor, its transform comes from the shared constants module, and the repo still passes `npm run typecheck && npm run lint && npm run build`
- [x] Add a Rapier left-wall collider component that mirrors the current pool boundary | AC: a Rapier fixed collider exists for the left wall, its transform comes from the shared constants module, and the repo still passes `npm run typecheck && npm run lint && npm run build`
- [x] Add a Rapier right-wall collider component that mirrors the current pool boundary | AC: a Rapier fixed collider exists for the right wall, its transform comes from the shared constants module, and the repo still passes `npm run typecheck && npm run lint && npm run build`
- [x] Add a Rapier front-wall collider component that mirrors the current pool boundary | AC: a Rapier fixed collider exists for the front wall, its transform comes from the shared constants module, and the repo still passes `npm run typecheck && npm run lint && npm run build`
- [x] Add a Rapier back-wall collider component that mirrors the current pool boundary | AC: a Rapier fixed collider exists for the back wall, its transform comes from the shared constants module, and the repo still passes `npm run typecheck && npm run lint && npm run build`
- [x] Add a Rapier fallback catch surface below the pool to preserve the current lower safety plane behavior | AC: a lower Rapier catch surface exists below the pool container, it is derived from shared constants or clearly documented values, and the repo still passes `npm run typecheck && npm run lint && npm run build`
- [x] Build typed instance descriptors for cube spawn position, initial rotation, size, and color | AC: the item list is transformed once into typed instance metadata that includes spawn position, initial rotation seed, scale source, and color data for every cube
- [/] Rework Rapier cube spawning into much faster grouped drops | AC: the Rapier items component still renders all portfolio cubes as instanced rigid bodies with size scaling from `getSize`, cubes still arrive above the pool in a few grouped drop zones instead of one long center stream, but the overall fall-in cadence is dramatically faster than the current pass, and the repo still passes `npm run typecheck && npm run lint && npm run build`
- [x] Restore hover-driven white highlight updates on the Rapier instanced cubes | AC: pointer hover detection works across the visible pool area instead of only near the screen center, hovered cubes reliably turn white again through the instanced color buffer without using Cannon hooks, and the repo still passes `npm run typecheck && npm run lint && npm run build`
- [x] Restore click selection and presenting-state wiring on the Rapier instanced cubes | AC: clicking a cube still updates `isPresenting`, event propagation remains contained to the cube interaction, and the repo still passes `npm run typecheck && npm run lint && npm run build`
- [x] Replace grace-period UI reveal with majority-in-pool readiness on Rapier | AC: sorting controls stay hidden until a clear majority of cubes have entered or settled inside the pool volume, the reveal fires exactly once without a fixed startup timer, and the repo still passes `npm run typecheck && npm run lint && npm run build`
- [x] Port global `sort` steering from Cannon velocity setters to Rapier body control | AC: when `sortOption === 'sort'`, each cube still moves toward its existing `sortingVelocity` target via Rapier rigid-body APIs and the repo still passes `npm run typecheck && npm run lint && npm run build`
- [x] Port set-filter steering from Cannon velocity setters to Rapier body control | AC: when a set filter is active, matching cubes still move toward the center target, non-matching cubes are still pushed away, and the behavior uses Rapier rigid-body APIs instead of Cannon
- [x] Cut `src/App.tsx` over from Cannon `Physics` to Rapier `Physics` and mount the Rapier bounds/items components | AC: `src/App.tsx` imports Rapier physics, the live scene uses the Rapier bounds and items implementations, and `npm run typecheck && npm run lint && npm run build` pass
- [x] Remove remaining Cannon imports and the `@react-three/cannon` dependency | AC: no source file imports `@react-three/cannon`, `package.json` and `package-lock.json` no longer include the package, and `npm run typecheck && npm run lint && npm run build` pass
- [/] Correct Rapier set-filter steering while preserving the working global sort speed | AC: when hovering a category, matching cubes still move toward the center area at a readable speed, non-matching cubes visibly move away from the center during the same interaction, the existing global `sort` behavior stays at the current good speed, the temporary Rapier Leva tuning panels remain absent, and the repo still passes `npm run typecheck && npm run lint && npm run build`
- [x] Record static migration handoff findings for manual runtime verification | AC: `npm run typecheck && npm run lint && npm run build` pass for the final migration state, remaining runtime caveats are written under `## Findings`, and the branch is ready for Rafal to do manual browser verification
- [x] Restore visible cube shadows in the active Rapier scene | AC: the instanced Rapier cubes cast readable shadows into the pool scene again without regressing the current look or interaction flow, and the repo still passes `npm run typecheck && npm run lint && npm run build`
- [x] Stop tracking generated Vite build output in git | AC: `dist/` is ignored, tracked generated `dist` artifacts are removed from the repo index, Vercel still builds from source with `dist` as the output directory, and the repo still passes `npm run typecheck && npm run lint && npm run build`

## Findings

- This repo has no project test files outside `node_modules`. Do not add tests, do not add test infrastructure, and do not run `npm run test` in this loop.
- The current physics implementation lives in `src/App.tsx`, `src/features/Items.tsx`, and `src/features/Bounds.tsx` via `@react-three/cannon`.
- Current behavior to preserve from the user-facing scene: cubes still fall into the pool from above, scale through `getSize`, highlight white on hover or presenting selection, unlock sorting only after the pool has materially filled, move toward `sortingVelocity` in global sort mode, and keep set filters readable without violently ejecting cubes from the center.
- The pool visual mesh in `src/features/Pool.tsx` is not the current collision source. The present containment shape comes from hidden bounds primitives in `src/features/Bounds.tsx` and should remain the behavioral reference for the first Rapier pass.
- The current hidden container is a floor box at `[0, -2.5, 0]` with size `[22, 1, 32]`, side walls at `x = ±15.5` with size `[9, 20, 26]`, front/back walls at `z = ±15.5` with size `[26, 20, 5]`, plus a lower safety plane near `y = -3`.
- Shared pool floor and wall transforms now live in `src/features/physics/constants.ts`, which is the intended source for both the existing Cannon bounds and upcoming Rapier collider components.
- The shared bounds module now also normalizes repeated wall offsets and sizes into named constants, so the Cannon path and upcoming Rapier bounds work can reuse one source of truth without reintroducing duplicate numbers.
- Shared item motion values now live in `src/features/physics/constants.ts` under `itemPhysicsConstants` (`spawnBaseHeight`, `spawnHeightStep`, `centerTarget`, `steeringStrength`) so upcoming Rapier item migration can reuse them directly.
- Rapier floor collider now exists in `src/features/rapier/FloorCollider.tsx`; `CuboidCollider` uses half-extents, so wall/catch colliders should convert shared size vectors with `/ 2` the same way.
- Rapier fallback catch surface bounds now live under `poolPhysicsBounds.catchSurface`, with position/size derived from the shared floor constants to keep the lower safety plane aligned with the existing pool dimensions.
- Typed per-cube descriptor metadata now lives in `src/features/physics/itemInstanceDescriptors.ts`; `Items` reads spawn position, deterministic initial rotation seed, scale source/scale vector, and color from this single transform instead of recomputing per concern.
- Rapier instanced cube spawning now lives in `src/features/rapier/RapierItems.tsx` via `InstancedRigidBodies`; per-instance `scale` is fed directly from `itemInstanceDescriptors` so `getSize` remains the single scaling source.
- One-time UI reveal for the Rapier path now lives on `src/features/rapier/FloorCollider.tsx` via `CuboidCollider` `onCollisionEnter`, with a 4-second startup grace window and a local single-fire guard instead of Cannon body-name checks.
- `InstancedRigidBodies` accepts a `ref` array of `RapierRigidBody | null`; steering logic can read `translation()` and call `setLinvel(...)` per cube without reintroducing Cannon APIs.
- Set-filter steering is now mirrored in `src/features/rapier/RapierItems.tsx`: matching indices from `sets[sortOption]` seek `itemPhysicsConstants.centerTarget`, while non-matching indices are pushed away with `setLinvel([-x, -1, -z], true)` to preserve the prior Cannon behavior.
- Active scene wiring now uses Rapier in `src/App.tsx` with `RapierItems` and `RapierBounds` (`src/features/rapier/RapierBounds.tsx` composes floor, four walls, and catch surface); remaining Cannon components are now unused cleanup targets for the next checklist item.
- Cannon cleanup is now complete: legacy `src/features/Items.tsx` and `src/features/Bounds.tsx` were removed, and `@react-three/cannon` no longer exists in `package.json`/`package-lock.json`.
- Rapier parity tuning now lives in `src/features/physics/constants.ts` under `rapierPhysicsConstants`; the scene now uses explicit world gravity, cube body mass/damping/canSleep/restitution/friction, and shared non-bouncy boundary collider friction/restitution instead of engine defaults.
- This checkout's `node_modules` can arrive with the wrong platform-specific `esbuild` package even when `package-lock.json` includes `esbuild-darwin-64`; restoring the cached macOS package into `node_modules/esbuild-darwin-64` unblocks `npm run build` on this host.
- Browser/runtime verification for this migration is manual and owned by Rafal outside the loop. Use static verification (`npm run typecheck && npm run lint && npm run build`) plus precise handoff notes in `## Findings`.
- Final static gate run succeeded on 2026-04-24 (UTC): `npm run typecheck && npm run lint && npm run build` all passed after Rapier cutover and Cannon removal.
- Manual browser handoff priorities for the next pass: validate that sorting controls stay hidden until most cubes have actually fallen into the pool, confirm grouped drop cadence feels faster than the current one-by-one stream, and check `sort` plus set-filter steering still keeps cubes bounded while settling calmly when hover ends.
- PR #5 review feedback reopens the Rapier tuning work: the next round should remove the fixed grace-period reveal, separate global sort behavior from set-filter gathering logic, and solve the center-clumping rebound without keeping temporary Leva tuning UI in the shipped scene.
- PR #5 review also flagged missing visible shadows in the active scene; the relevant shadow path currently runs through `Canvas shadows`, `src/features/Lights.tsx`, `src/features/Pool.tsx`, and the transmissive instanced-cube material in `src/features/rapier/RapierItems.tsx`.
- `dist/` is currently tracked in git on this branch and on `master`; `vercel.json` already builds from source into `dist`, so ignoring generated build artifacts should remain compatible with the current deploy setup.
- Rapier steering tuning now exposes separate live Leva sliders in `src/features/rapier/RapierItems.tsx` (`sortPull`, `setMatchSeek`, `setMissRepel`), with defaults in `rapierPhysicsConstants.steering`; the panel is mounted in `src/App.tsx`.
- Restoring visible Rapier shadows required two changes: explicit instanced cube shadow depth/distance materials in `src/features/rapier/RapierItems.tsx` (to keep shadow casting reliable with transmissive `meshPhysicalMaterial`) and a tuned `castShadow` directional key light in `src/features/Lights.tsx`.
- User follow-up feedback after the first Rapier tuning pass: the pool still feels too slippery, so the next iteration should extend Leva beyond steering-only controls and expose pool collider friction/restitution plus falling-cube rigid-body friction/restitution/damping from the shared Rapier constants path.
- Current Leva tuning is still localized to `src/features/rapier/RapierItems.tsx`, while pool contact properties are statically read from `rapierPhysicsConstants.bounds` by the Rapier collider components and cube contact properties are statically read from `rapierPhysicsConstants.items` by `RapierItems.tsx`.
- User follow-up feedback also says hover/highlight only works for cubes near the screen center; because the white-color buffer update still keys off `hovered` and `isPresenting` in `src/features/rapier/RapierItems.tsx`, the next pass should focus on instanced-mesh pointer hit testing rather than the color update itself.
- Instanced hover hit testing in `src/features/rapier/RapierItems.tsx` now recomputes `InstancedMesh` bounding spheres each frame (`computeBoundingSphere()`) and uses `onPointerMove`/`onPointerOut` for instance-level hover state, which restores white highlight detection away from screen center as cubes move under Rapier.
- Rapier contact tuning controls are now live in Leva: `src/features/rapier/RapierBounds.tsx` owns shared pool friction/restitution sliders consumed by all Rapier boundary colliders, and `src/features/rapier/RapierItems.tsx` owns falling-cube friction/restitution/linear+angular damping sliders alongside the existing steering controls.
- Rafal's current calibrated Leva values for the next tuning pass are: `cubeFriction=1`, `cubeRestitution=0.6`, `cubeLinearDamping=1`, `cubeAngularDamping=3`, `sortPull=8`, `setMatchSeek=20`, `setMissRepel=20`, `poolFriction=2`, `poolRestitution=0.5`.
- Rafal's calibrated values are now baked into `rapierPhysicsConstants` defaults in `src/features/physics/constants.ts`, and both `RapierBounds` and `RapierItems` still expose the same Leva controls for live retuning.
- Rafal rejected the fixed 4-second grace-period gate in `src/features/rapier/FloorCollider.tsx`; the next pass should reveal sorting controls only after most cubes have actually fallen into the pool rather than after elapsed wall-clock time.
- Because `FloorCollider.tsx` currently sees one floor contact at a time, majority-in-pool readiness likely needs cube-level accounting in `RapierItems.tsx`, shared Zustand state, or equivalent logic instead of another collider-local timer.
- Majority-in-pool UI readiness now runs in `src/features/rapier/RapierItems.tsx` by counting rigid-body translations inside the existing pool interior AABB derived from shared wall/floor constants; UI reveals once when `>50%` of cubes are within that volume, with no startup timer.
- Grouped spawn cadence now lives in `src/features/physics/itemInstanceDescriptors.ts`: cubes are distributed across four fixed x/z group centers with deterministic per-layer offsets, and vertical spacing advances by group layers (`Math.floor(index / 4)`) instead of one long per-index center stack.
- `src/features/rapier/RapierItems.tsx` currently normalizes matching cubes toward one exact `centerTarget` and applies full `setLinvel(...)` until sorting stops, which matches Rafal's report that cubes jump away from the middle once those forces disappear.
- Rafal wants the temporary Rapier Leva tuning panels removed in this next pass; once the new spawn, reveal, and steering behavior is tuned, keep the defaults in shared constants instead of live runtime sliders.
- Rapier retune pass is now static (no Leva panel in `src/App.tsx`, no `useControls` in Rapier bounds/items): matching set-filter cubes steer toward deterministic offsets inside a shared center radius, non-matching cubes only get outward push while inside that center zone, and active/release motion uses velocity blending (`activeLerp`/`settleLerp`) to avoid abrupt spring-away when UI hover exits.
- Rafal confirmed the grouped spawn positions are directionally correct, but the current drop still reads as far too slow; the next pass should keep the multi-zone pattern and increase perceived fall speed very aggressively rather than reverting to a single stream.
- Rafal also confirmed the current global `sort` behavior feels correct, so the next pass should not retune `sortPull`/`maxSortSpeed` unless required for a direct bug fix.
- The current non-match repel path in `src/features/rapier/RapierItems.tsx` only pushes cubes outward once they are already inside `centerAreaRadius`; because Rafal reports that category hover does not visibly move unrelated cubes away, the next pass should revisit that gating or the repel target itself rather than reworking the matching-center path first.
