# Feature Delivery Progress

- [x] Add explicit card typography class structure | AC: selected item card rendering has distinct family label, title, subtitle, metadata, field key, field value, and link class names or equivalent Tailwind class groupings, no visual behavior changes beyond typography hooks, `npm run typecheck && npm run lint && npm run build` passes
- [x] Render card family label with presentation uppercase styling | AC: family label renders uppercase via presentation styling rather than modifying item data, family labels remain sourced from item family values, `npm run typecheck && npm run lint && npm run build` passes
- [x] Render card title with presentation uppercase styling | AC: selected item title renders uppercase via presentation styling, source item title strings are unchanged, `npm run typecheck && npm run lint && npm run build` passes
- [x] Render card subtitle with presentation uppercase styling | AC: selected item subtitles render uppercase via presentation styling when present, source item subtitle strings are unchanged, cards without subtitles still render without empty subtitle elements, `npm run typecheck && npm run lint && npm run build` passes
- [x] Render card field keys with bold font | AC: card field keys render in a bold or semibold text style, card field values keep normal readable text styling, array field values still render joined as before, `npm run typecheck && npm run lint && npm run build` passes
- [x] Render card links with underlined text | AC: selected item links render underlined, link href/target/rel behavior is preserved, cards without links still render no link row, `npm run typecheck && npm run lint && npm run build` passes
- [x] Add compact CURRENT badge for current items | AC: items with `current: true` render a compact uppercase CURRENT badge in their selected card, the badge is visually separate from date/location text, `npm run typecheck && npm run lint && npm run build` passes
- [x] Omit CURRENT badge for non-current items | AC: items without `current: true` render no CURRENT badge, no portfolio data shape changes are required, `npm run typecheck && npm run lint && npm run build` passes
- [x] Extract regular DOM selected-card overlay component | AC: selected item card presentation is implemented as a React DOM component rendered outside the Three.js `Html` layer, the component receives or selects the active item without changing item source data, `npm run typecheck && npm run lint && npm run build` passes
- [x] Wire selected-card overlay into App outside Canvas | AC: selected item cards render as regular DOM siblings of the Canvas rather than inside Drei `Html`, the old selected-card Drei Html path is removed or disabled, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Make right-side card panel scrollable | AC: the selected-card panel is positioned on the right side on desktop, long card content scrolls inside the panel, viewport-level scrolling is not required for card content, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Preserve mobile-safe card overlay layout | AC: selected-card panel remains readable within small viewport widths, text does not overflow its panel horizontally, close control remains accessible, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Keep closing card tied to selection state | AC: closing the selected-card panel sets `isPresenting` to `null`, closing also leaves sort/gather state in a safe idle state, clicking scene items can still open cards, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Drive left-stage camera position from selection state | AC: opening an item card shifts the scene view so items remain visible on the left side of the viewport, closing the card restores the previous default camera position, camera state remains controlled from selection state, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Drive controls presentation mode from selection state | AC: opening an item card disables the previous rotating default view, closing the card restores the previous rotating default view, existing pan/zoom/rotate constraints remain intentional, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Consolidate creative item family defaults | AC: `src/data/creative.ts` uses a shared local spread pattern like `src/data/ai.ts` for repeated family/color/sortingVelocity defaults, exported creative item count and generated ids remain unchanged, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Consolidate project item family defaults | AC: `src/data/projects.ts` uses a shared local spread pattern like `src/data/ai.ts` for repeated family/color/sortingVelocity defaults, exported project item count and generated ids remain unchanged, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Consolidate stack item family defaults | AC: `src/data/technologies.ts` uses a shared local spread pattern like `src/data/ai.ts` for repeated family/color/sortingVelocity defaults, exported stack item count and generated ids remain unchanged, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Consolidate career item family defaults consistently | AC: `src/data/experience.ts` keeps or updates its shared default mapping so family/color/sortingVelocity boilerplate remains centralized, exported career item count and generated ids remain unchanged, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Move AI full-sort target to a distinct quadrant | AC: AI family `sortingVelocity` no longer shares the stack target quadrant, full sort mode keeps project/stack/creative/career existing family separation, item source ids and counts remain unchanged, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Centralize family visual geometry dimensions | AC: family geometry dimensions remain centralized in `RapierItems.tsx`, there is a clear local mapping or helper for family geometry values, rendered family shapes remain the same except where later tasks explicitly resize creative, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Centralize family collider dimensions | AC: family collider dimensions remain centralized in `RapierItems.tsx`, there is a clear local mapping or helper for family collider values, collider component choices remain appropriate for each family, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Normalize creative medium visual footprint | AC: creative medium geometry dimensions no longer exceed project large visual footprint, source data size labels remain unchanged, non-creative geometry dimensions are unchanged unless needed for centralization, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Normalize creative medium collider footprint | AC: creative medium collider dimensions no longer exceed project large collider footprint, source data size labels remain unchanged, collider dimensions stay centralized in `RapierItems.tsx`, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Define size-based mass mapping | AC: physics code defines a size-to-mass mapping with `s < m < l`, `m` and `l` masses are both greater than current baseline `1`, source item data is not changed to store mass, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Assign mass per item instance | AC: rigid body instances derive mass from item size per instance, mass is assigned through instance/body props rather than a single family-wide baseline, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Remove hover-triggered gather state from menu controls | AC: category/project/sort controls no longer set sort or gather state on mouse enter, controls no longer clear sort or gather state on mouse leave, hover styling if any remains purely visual, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Add click-triggered active gather state | AC: clicking a category/project/sort control starts an active gather for that option, clicking another control replaces the active gather option, global state records enough timing information for expiry, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Preserve menu visibility rules during card presentation | AC: menu controls still hide while an item card is presenting if that is the current intended behavior, card opening clears active gather state or leaves it in a documented safe state, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Add default four-second gather duration | AC: active gather lasts 4000ms by default, the duration is centralized as a constant rather than duplicated magic numbers, gather state clears automatically after expiry, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Implement decaying gather envelope | AC: gather steering computes a decay factor that is stronger early and weaker later across the active gather window, the factor reaches an idle or near-idle value by expiry, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Apply decaying steering to matched gather items | AC: matched category/project items receive stronger steering early and weaker steering later, matched item target distribution remains centered around the existing center target area, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Apply decaying repulsion to non-matching gather items | AC: non-matching category/project items are strongly repelled early and less strongly later using the same decay envelope, existing fallback direction handling remains safe when an item is at center, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Apply decaying steering to full sort mode | AC: clicking the sort control starts a decaying full-sort gather, full sort uses item `sortingVelocity` targets and weakens over the same four-second envelope, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Clear expired gather state from frame loop | AC: expired gather state clears automatically without requiring another click or hover event, the frame loop returns to idle steering after expiry, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Final combined verification sweep | AC: no task introduced committed tests or test infrastructure, no dev server is started, `npm run typecheck && npm run lint && npm run build` passes on the final combined state

---

## Findings

(critical discoveries only)
