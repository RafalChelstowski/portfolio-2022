# Feature Delivery Progress

- [x] Add explicit card typography class structure | AC: selected item card rendering has distinct family label, title, subtitle, metadata, field key, field value, and link class names or equivalent Tailwind class groupings, no visual behavior changes beyond typography hooks, `npm run typecheck && npm run lint && npm run build` passes
- [x] Render card family label with presentation uppercase styling | AC: family label renders uppercase via presentation styling rather than modifying item data, family labels remain sourced from item family values, `npm run typecheck && npm run lint && npm run build` passes
- [x] Render card title with presentation uppercase styling | AC: selected item title renders uppercase via presentation styling, source item title strings are unchanged, `npm run typecheck && npm run lint && npm run build` passes
- [x] Render card subtitle with presentation uppercase styling | AC: selected item subtitles render uppercase via presentation styling when present, source item subtitle strings are unchanged, cards without subtitles still render without empty subtitle elements, `npm run typecheck && npm run lint && npm run build` passes
- [x] Render card field keys with bold font | AC: card field keys render in a bold or semibold text style, card field values keep normal readable text styling, array field values still render joined as before, `npm run typecheck && npm run lint && npm run build` passes
- [x] Render card links with underlined text | AC: selected item links render underlined, link href/target/rel behavior is preserved, cards without links still render no link row, `npm run typecheck && npm run lint && npm run build` passes
- [x] Add compact CURRENT badge for current items | AC: items with `current: true` render a compact uppercase CURRENT badge in their selected card, the badge is visually separate from date/location text, `npm run typecheck && npm run lint && npm run build` passes
- [x] Omit CURRENT badge for non-current items | AC: items without `current: true` render no CURRENT badge, no portfolio data shape changes are required, `npm run typecheck && npm run lint && npm run build` passes
- [x] Extract regular DOM selected-card overlay component | AC: selected item card presentation is implemented as a React DOM component rendered outside the Three.js `Html` layer, the component receives or selects the active item without changing item source data, `npm run typecheck && npm run lint && npm run build` passes
- [x] Wire selected-card overlay into App outside Canvas | AC: selected item cards render as regular DOM siblings of the Canvas rather than inside Drei `Html`, the old selected-card Drei Html path is removed or disabled, `npm run typecheck && npm run lint && npm run build` passes
- [x] Make right-side card panel scrollable | AC: the selected-card panel is positioned on the right side on desktop, long card content scrolls inside the panel, viewport-level scrolling is not required for card content, `npm run typecheck && npm run lint && npm run build` passes
- [x] Preserve mobile-safe card overlay layout | AC: selected-card panel remains readable within small viewport widths, text does not overflow its panel horizontally, close control remains accessible, `npm run typecheck && npm run lint && npm run build` passes
- [x] Keep closing card tied to selection state | AC: closing the selected-card panel sets `isPresenting` to `null`, closing also leaves sort/gather state in a safe idle state, clicking scene items can still open cards, `npm run typecheck && npm run lint && npm run build` passes
- [x] Drive left-stage camera position from selection state | AC: opening an item card shifts the scene view so items remain visible on the left side of the viewport, closing the card restores the previous default camera position, camera state remains controlled from selection state, `npm run typecheck && npm run lint && npm run build` passes
- [x] Drive controls presentation mode from selection state | AC: opening an item card disables the previous rotating default view, closing the card restores the previous rotating default view, existing pan/zoom/rotate constraints remain intentional, `npm run typecheck && npm run lint && npm run build` passes
- [x] Consolidate creative item family defaults | AC: `src/data/creative.ts` uses a shared local spread pattern like `src/data/ai.ts` for repeated family/color/sortingVelocity defaults, exported creative item count and generated ids remain unchanged, `npm run typecheck && npm run lint && npm run build` passes
- [x] Consolidate project item family defaults | AC: `src/data/projects.ts` uses a shared local spread pattern like `src/data/ai.ts` for repeated family/color/sortingVelocity defaults, exported project item count and generated ids remain unchanged, `npm run typecheck && npm run lint && npm run build` passes
- [x] Consolidate stack item family defaults | AC: `src/data/technologies.ts` uses a shared local spread pattern like `src/data/ai.ts` for repeated family/color/sortingVelocity defaults, exported stack item count and generated ids remain unchanged, `npm run typecheck && npm run lint && npm run build` passes
- [x] Consolidate career item family defaults consistently | AC: `src/data/experience.ts` keeps or updates its shared default mapping so family/color/sortingVelocity boilerplate remains centralized, exported career item count and generated ids remain unchanged, `npm run typecheck && npm run lint && npm run build` passes
- [x] Move AI full-sort target to a distinct quadrant | AC: AI family `sortingVelocity` no longer shares the stack target quadrant, full sort mode keeps project/stack/creative/career existing family separation, item source ids and counts remain unchanged, `npm run typecheck && npm run lint && npm run build` passes
- [x] Centralize family visual geometry dimensions | AC: family geometry dimensions remain centralized in `RapierItems.tsx`, there is a clear local mapping or helper for family geometry values, rendered family shapes remain the same except where later tasks explicitly resize creative, `npm run typecheck && npm run lint && npm run build` passes
- [x] Centralize family collider dimensions | AC: family collider dimensions remain centralized in `RapierItems.tsx`, there is a clear local mapping or helper for family collider values, collider component choices remain appropriate for each family, `npm run typecheck && npm run lint && npm run build` passes
- [x] Normalize creative medium visual footprint | AC: creative medium geometry dimensions no longer exceed project large visual footprint, source data size labels remain unchanged, non-creative geometry dimensions are unchanged unless needed for centralization, `npm run typecheck && npm run lint && npm run build` passes
- [x] Normalize creative medium collider footprint | AC: creative medium collider dimensions no longer exceed project large collider footprint, source data size labels remain unchanged, collider dimensions stay centralized in `RapierItems.tsx`, `npm run typecheck && npm run lint && npm run build` passes
- [x] Define size-based mass mapping | AC: physics code defines a size-to-mass mapping with `s < m < l`, `m` and `l` masses are both greater than current baseline `1`, source item data is not changed to store mass, `npm run typecheck && npm run lint && npm run build` passes
- [x] Assign mass per item instance | AC: rigid body instances derive mass from item size per instance, mass is assigned through instance/body props rather than a single family-wide baseline, `npm run typecheck && npm run lint && npm run build` passes
- [x] Remove hover-triggered gather state from menu controls | AC: category/project/sort controls no longer set sort or gather state on mouse enter, controls no longer clear sort or gather state on mouse leave, hover styling if any remains purely visual, `npm run typecheck && npm run lint && npm run build` passes
- [x] Add click-triggered active gather state | AC: clicking a category/project/sort control starts an active gather for that option, clicking another control replaces the active gather option, global state records enough timing information for expiry, `npm run typecheck && npm run lint && npm run build` passes
- [x] Preserve menu visibility rules during card presentation | AC: menu controls still hide while an item card is presenting if that is the current intended behavior, card opening clears active gather state or leaves it in a documented safe state, `npm run typecheck && npm run lint && npm run build` passes
- [x] Add default four-second gather duration | AC: active gather lasts 4000ms by default, the duration is centralized as a constant rather than duplicated magic numbers, gather state clears automatically after expiry, `npm run typecheck && npm run lint && npm run build` passes
- [x] Implement decaying gather envelope | AC: gather steering computes a decay factor that is stronger early and weaker later across the active gather window, the factor reaches an idle or near-idle value by expiry, `npm run typecheck && npm run lint && npm run build` passes
- [x] Apply decaying steering to matched gather items | AC: matched category/project items receive stronger steering early and weaker steering later, matched item target distribution remains centered around the existing center target area, `npm run typecheck && npm run lint && npm run build` passes
- [x] Apply decaying repulsion to non-matching gather items | AC: non-matching category/project items are strongly repelled early and less strongly later using the same decay envelope, existing fallback direction handling remains safe when an item is at center, `npm run typecheck && npm run lint && npm run build` passes
- [x] Apply decaying steering to full sort mode | AC: clicking the sort control starts a decaying full-sort gather, full sort uses item `sortingVelocity` targets and weakens over the same four-second envelope, `npm run typecheck && npm run lint && npm run build` passes
- [x] Clear expired gather state from frame loop | AC: expired gather state clears automatically without requiring another click or hover event, the frame loop returns to idle steering after expiry, `npm run typecheck && npm run lint && npm run build` passes
- [x] Final combined verification sweep | AC: no task introduced committed tests or test infrastructure, no dev server is started, `npm run typecheck && npm run lint && npm run build` passes on the final combined state
- [x] Raise selected-card overlay above site chrome | AC: selected-card overlay stacking is higher than the header/menu/loader chrome, the close button remains clickable while a card is open, pointer-events still pass through only outside the interactive card panel, `npm run typecheck && npm run lint && npm run build` passes
- [x] Anchor selected-card panel to the right-side stage | AC: desktop selected-card panel opens on the right side of the viewport rather than at the top center, panel height is bounded by the viewport with internal scrolling for long content, mobile layout remains readable with the close button visible without horizontal overflow, `npm run typecheck && npm run lint && npm run build` passes
- [x] Keep header chrome from occluding card actions | AC: when a card is presenting, the header/menu layer does not overlap or intercept pointer events for the card panel or close button, the existing header appearance is preserved when no card is open, `npm run typecheck && npm run lint && npm run build` passes
- [x] Shift Three.js scene into a left-stage composition while presenting | AC: opening a card visibly composes the scene toward the left side of the viewport with the selected item cluster remaining visible beside the right-side card, closing the card restores the previous centered rotating view, camera/control state remains driven by selection state, `npm run typecheck && npm run lint && npm run build` passes
- [x] Final selected-card cleanup verification sweep | AC: no task introduced committed tests or test infrastructure, no dev server is started, selected-card close behavior is not blocked by overlay stacking, and `npm run typecheck && npm run lint && npm run build` passes on the final combined state
- [x] Constrain selected-card overlay to the right half on desktop | AC: on desktop-width viewports the selected-card overlay reserves a right-half stage for the card panel instead of allowing the panel to read as a top-half overlay, the card remains right-aligned inside that stage, mobile layout remains readable, `npm run typecheck && npm run lint && npm run build` passes
- [x] Add generous vertical margins to selected-card panel | AC: selected-card panel has visible top and bottom breathing room on desktop, panel height is bounded by the viewport with internal scrolling for overflow content, close button remains visible and clickable, `npm run typecheck && npm run lint && npm run build` passes
- [x] Increase presentation scene zoom modestly | AC: while a card is open the Three.js scene appears slightly more zoomed in than the current presentation state without hiding the selected item cluster behind the right-side card panel, closing the card restores the default camera/zoom/view offset, `npm run typecheck && npm run lint && npm run build` passes
- [x] Final right-panel presentation verification sweep | AC: no task introduced committed tests or test infrastructure, no dev server is started, right-side card panel and presentation camera behavior match the requested layout, and `npm run typecheck && npm run lint && npm run build` passes on the final combined state
- [x] Remove visible close-button background strip | AC: selected-card close control no longer introduces a visible white strip or opacity seam over card content, close control remains readable and clickable while card content scrolls, `npm run typecheck && npm run lint && npm run build` passes
- [x] Vertically center desktop selected-card panel | AC: on desktop-width viewports the right-side selected-card panel is vertically centered within the viewport with generous top and bottom margin, long content still scrolls inside the card panel, `npm run typecheck && npm run lint && npm run build` passes
- [x] Convert mobile selected-card layout to bottom sheet | AC: on mobile-width viewports the selected-card panel spans the full viewport width near the bottom, uses about 30% of viewport height with internal scrolling, and does not cover the entire screen, `npm run typecheck && npm run lint && npm run build` passes
- [x] Preserve natural subtitle casing in selected cards | AC: selected-card subtitles no longer render uppercase from presentation styling, source subtitle strings remain unchanged, title and family label uppercase presentation styling remains unchanged, `npm run typecheck && npm run lint && npm run build` passes
- [ ] Final selected-card responsive polish verification sweep | AC: no task introduced committed tests or test infrastructure, no dev server is started, desktop right-panel centering, mobile bottom-sheet sizing, close control styling, and subtitle casing match the requested behavior, and `npm run typecheck && npm run lint && npm run build` passes on the final combined state

---

## Findings

(critical discoveries only)
- Current Vite/esbuild setup rejects TypeScript `satisfies`; use explicit annotations compatible with the existing build toolchain.
- Collider footprint comparisons must include per-item scale from `getSize`; creative medium uses the base family collider radius multiplied by medium scale.
- The selected-card overlay currently uses `z-10` while the header wrapper also uses `z-10` and renders later in `App.tsx`; equal z-index plus DOM order can let the header sit above the card and block the close button.
- The current presentation camera only changes position; for the desired right-card/left-scene layout, the cleanup pass should explicitly compose the scene for a left-stage presentation state instead of relying on the card overlay alone.
- The selected-card pane still uses a full-screen overlay with a `w-full max-w-96` panel; the next pass should create an explicit right-half desktop stage with top/bottom margins rather than relying only on `justify-end`.
